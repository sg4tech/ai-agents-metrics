# Local Linear Setup

This repository uses the `codex-metrics workspace` team for Linear intake.

Current workflow:

- `Backlog` / `backlog`
- `Todo` / `unstarted`
- `In Progress` / `started`
- `In Review` / `started`
- `Done` / `completed`
- `Canceled` / `canceled`

Linear workflow settings are team-specific, so this setup should be verified against the team settings if it ever changes.
